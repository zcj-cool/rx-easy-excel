package com.rxliuli.rxeasyexcel.domain.select;

/**
 * Excel 的类型，主要关系到 Excel 单元格的显示
 *
 * @author rxliuli
 */
public enum ExcelColumnType {
    /**
     * 默认为普通的文本
     */
    TEXT,
    /**
     * 下拉框类型
     */
    SELECT,
}
